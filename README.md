# midi2websockets
Take local midi messages (like note on, note off) and send out to remote server via websockets.
Uses Node.js

# Node Version (IMPORTANT)
Make sure to use something like NVM to change Node Version to 18.4.0. Midi node packages don't work well with newer versions of Node above 18.4.0.
